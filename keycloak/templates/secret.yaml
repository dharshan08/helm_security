apiVersion: v1
kind: Secret
metadata:
  name: keycloak-secret
  namespace: keycloak
type: Opaque
data:
  admin-password: {{ "admin" | b64enc }}
  db-password: {{ "keycloak" | b64enc }}
  keystore-password: {{ "changeit" | b64enc }}
